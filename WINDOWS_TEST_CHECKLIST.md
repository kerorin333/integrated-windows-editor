# Windows実機検証チェックリスト

## 事前準備

| 項目 | 確認 |
|---|---|
| Windows 10または11 | □ |
| Node.js LTS | □ |
| pnpm | □ |
| プロジェクトをローカルへ展開 | □ |
| `pnpm install`完了 | □ |

## 開発起動

```powershell
pnpm desktop:dev
```

起動後、本文入力、章・節・シーンの展開、作品棚の折りたたみ、縦書き、ルビプレビュー、DOCX出力を確認します。

## 互換保存

1. 「互換保存」を選択し、任意のフォルダーを指定する。
2. `manuscript/`、`.archive-desk/project.json`、`README.md`が作成されることを確認する。
3. VS Codeでルートフォルダーを開き、本文Markdownを編集する。
4. Obsidianで同じルートフォルダーを保管庫として開き、本文が表示されることを確認する。
5. 統合Windowsエディタで「互換を開く」を選び、構成情報が読み込まれることを確認する。

## 配布版

```powershell
pnpm desktop:win
```

`release/`にNSISインストーラーとポータブル版が生成されることを確認します。インストール版とポータブル版の両方で、既存のプロジェクトを開き、保存、再起動、再読込を確認します。

## 合格条件

原稿がUTF-8で保存され、VS CodeとObsidianの双方で本文を開けること、アプリ再起動後も構成情報が失われないこと、外部編集を検知できること、DOCXと完成原稿の書き出しが成功することを合格条件とします。
