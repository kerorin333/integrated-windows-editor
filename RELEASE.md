# 統合Windowsエディタ 即利用版

## 現行版

**バージョン:** 0.3.0  
**位置づけ:** 即利用できる試作安定版  
**開発方式:** React + Electron  
**保存方式:** Markdown本文 + `.archive-desk/project.json`

## すぐに使う方法

Windows環境でプロジェクトのルートを開き、次のコマンドを実行します。

```powershell
pnpm install
pnpm desktop:dev
```

配布用のインストーラーとポータブル版を作成する場合は、次を実行します。

```powershell
pnpm desktop:win
```

`release/`に生成されたインストーラーを使う場合は、既存プロジェクトのコピーを一つ作ってから試してください。原稿そのものはMarkdownファイルなので、問題があればVS CodeやObsidianから直接開けます。

## 使いながら改定する方法

改定は、現在の安定版を残したまま新しいチェックポイントとして保存します。本文ファイルを変更する場合は、まずプロジェクトフォルダーを複製し、複製側で作業してください。統合Windowsエディタ側の構成変更は、アプリ内スナップショットと開発側チェックポイントの両方を使って復元可能にします。

| 状態 | 扱い |
|---|---|
| 安定版 | 日常の執筆に使用する版 |
| 作業版 | 新機能やUI変更を試す版 |
| 検証版 | Windows実機で動作確認する版 |
| 次期安定版 | 検証後に採用する版 |

各改定では、変更内容、確認した操作、未確認の項目を`CHANGELOG.md`へ記録します。安定版へ戻す場合は、プロジェクト管理画面のバージョン履歴から前のチェックポイントを選択します。

## 互換性の範囲

VS CodeとObsidianでは、`manuscript/`以下のMarkdown本文を通常のファイルとして編集できます。統合Windowsエディタ固有の進捗、アウトライン順、状態、メタデータは`.archive-desk/project.json`に保持されます。本文の編集は互換性がありますが、外部アプリで構成情報を変更した場合は、統合Windowsエディタ側で再読込が必要です。

## 0.3.0で追加された運用機能

アプリ上部の履歴アイコンから、現在版、変更履歴、復元手順を確認できます。互換フォルダーを開いた後は、外部でMarkdownが変更されると通知が表示され、「保持」または「再読込」を選択できます。現在の編集を残したい場合は「保持」を選び、外部ファイルを反映したい場合は「再読込」を選びます。

## 今後の改定方針

今後の改定は、既存の保存形式を壊さず、Markdown本文とプロジェクトJSONのバージョン番号を維持しながら追加します。互換性を壊す変更が必要な場合は、変換処理を追加し、旧版のバックアップを残してから移行します。
