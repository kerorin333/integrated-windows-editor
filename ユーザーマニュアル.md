# 統合Windowsエディタ 利用マニュアル

## 1. このアプリについて

「統合Windowsエディタ」は、長編小説の構成づくり、本文執筆、進捗管理、互換保存、完成原稿の書き出しを一つのWindowsアプリにまとめた執筆環境です。

作品を章・節・シーンの階層で管理しながら本文を書けるため、長編作品で起こりやすい「いま何を書いているのか分からなくなる」「章の順番を変更すると管理が難しい」「複数ファイルを最後に結合するのが面倒」といった問題を減らせます。

本文の保存形式は標準的なMarkdownです。互換フォルダーとして保存した原稿は、専用アプリがなくてもVS CodeやObsidianで開いて編集できます。

> 本マニュアルは、現段階版（0.3系・チェックポイント053084dd相当）を基準にしています。Windows実機での最終的なIME、外部エディタ連携、DOCX出力の確認は、利用環境でも実施してください。

## 2. Windows版の導入と起動

### 2.1 ポータブル版を使う場合

ビルド後のポータブル版は、通常次の場所に生成されます。

```text
release\\統合Windowsエディタ-0.3.0-x64.exe
```

このファイルはインストール作業を必要としません。ダブルクリックして起動してください。原稿データはアプリ本体のフォルダーとは別の場所へ保存することを推奨します。

Windows Defender SmartScreenの警告が表示される場合があります。配布元とファイルを確認したうえで、必要に応じて「詳細情報」から実行してください。

### 2.2 Windows上でビルドする場合

プロジェクトフォルダーを開き、PowerShellで次を実行します。

```powershell
cd "C:\Users\PC\Desktop\統合Windowsエディタアプリ\integrated-windows-editor"
pnpm.cmd install
pnpm.cmd check
pnpm.cmd desktop:win:staged
```

完了後、生成物を確認します。

```powershell
Get-ChildItem .\release -Filter *.exe
```

`統合Windowsエディタ-0.3.0-x64.exe`が表示されれば、ポータブル実行ファイルが生成されています。ビルド途中で古い実行ファイルを起動しないよう、生成時刻を確認してから起動してください。

## 3. 画面構成

画面は、おおむね五つの領域で構成されています。

| 領域 | 主な役割 |
|---|---|
| 作品棚 | 複数作品の切り替え、新しい作品の追加、作品ごとの進捗確認 |
| 構成アウトライン | 部・章・節・シーンの階層管理、検索、並べ替え、展開・折りたたみ |
| 本文エディタ | 選択した章・節・シーンの本文編集、縦書き、プレビュー |
| 編集控え | 目標文字数、締切、日次目標、達成見込み、日別履歴、メタデータ |
| 上部メニューとツールバー | 新規作成、保存、互換保存、表示切り替え、書き出し |

中央の本文エディタを広く使いたい場合は、作品棚の折りたたみボタンを押してください。作品棚を閉じると、本文編集領域が広がります。

## 4. 作品の切り替えと追加

### 4.1 作品を切り替える

左側の「作品棚」から作品カードをクリックします。選択した作品に応じて、本文、アウトライン、作品設定、目標文字数、締切、執筆履歴が切り替わります。

現段階版には確認用の作品が複数登録されています。作品カードには、作品名、ジャンル、締切までの日数、進捗率、現在の文字数が表示されます。

### 4.2 新しい作品を追加する

作品棚の「新しい作品」または作品棚見出し付近の追加ボタンを押します。新しい作品が追加され、初期アウトラインを編集できる状態になります。

作品名、ジャンル、応募先、目標文字数、締切日は、上部の設定ボタンから変更してください。

> 作品棚では、作品のアーカイブと完全削除も利用できます。アーカイブは原稿を残したまま作品棚から一時的に隠す機能で、後から復元できます。完全削除は確認ダイアログを表示してから実行してください。

## 5. アウトラインを作る

### 5.1 階層を確認する

アウトラインには、部・章・シーンなどの階層が表示されます。各項目には種別、概要、状態、文字数、子項目数が表示されるため、章が増えても構成を把握しやすくなっています。

「全展開」を押すと下位項目をすべて表示し、「全折りたたみ」を押すと上位項目中心の表示に戻せます。検索欄へ章名やシーン名を入力すると、タイトルと概要から絞り込めます。

### 5.2 項目を選択する

アウトラインの項目をクリックすると、中央の本文エディタと右側のメタデータ欄が選択項目に切り替わります。本文を書く対象を変更する場合は、先にアウトラインから章・節・シーンを選択してください。

### 5.3 順番を変更する

項目の上下移動ボタン、またはドラッグ操作を使って章・節・シーンの順序を変更できます。順序を変更すると、互換フォルダーへ保存する際のファイル番号と書き出し順にも反映されます。誤操作をした場合は、元に戻す・やり直す操作を利用してください。

### 5.4 シーンを追加する

アウトライン下部の「シーンを追加」を押すと、選択項目の下に新しいシーンが追加されます。追加後、タイトル、概要、状態、目標文字数、本文を編集してください。

## 6. 本文を書く

中央の本文欄へ文章を入力します。本文はMarkdownとして扱われ、通常の改行をそのまま保存できます。入力した文字数は作品の進捗計算に反映されます。

上部の「縦書き」を押すと縦書き表示へ切り替わります。もう一度押すと横書きへ戻ります。「プレビュー」を押すと編集欄からプレビュー表示へ切り替わります。

ルビは、次の記法で入力できます。

```text
｜海鳴り《うみなり》
```

プレビューでは、対象語の上に読みが表示されます。書き出し時には、出力形式に応じて読みやすい表記へ変換されます。

## 7. 作品設定と進捗管理

### 7.1 設定画面を開く

画面右上の設定ボタンを押すと、執筆設定画面が開きます。次の項目を設定できます。

| 設定項目 | 用途 |
|---|---|
| 作品名 | 作品棚、ヘッダー、保存情報に表示する名称 |
| ジャンル | 作品の分類や執筆ワークスペースの表示 |
| 応募先 | 応募予定の賞、媒体、企画などの管理 |
| 作品メモ | 応募要項、作品方針、注意事項などの記録 |
| 作品全体の目標文字数 | 作品全体の進捗率と残り文字数の基準 |
| 締切日 | 残り日数と締切通知の基準 |
| 1日あたりの執筆目標 | 日々の執筆ペースを確認する基準 |

入力後、「設定を保存」を押してください。設定はプロジェクトJSONと互換フォルダー保存に含まれます。

### 7.2 締切達成見込み

右側の「締切達成見込み」カードでは、直近の執筆履歴から平均文字数を計算し、残り文字数を現在の平均ペースで書き終えるまでの日数を表示します。

表示は次の三種類です。

| 表示 | 意味 |
|---|---|
| 記録待ち | 日別執筆履歴がまだ十分に蓄積されていない状態 |
| 達成見込み | 直近平均ペースが、締切までに必要なペース以上の状態 |
| 要ペースアップ | 直近平均ペースが、締切までに必要なペースを下回る状態 |

この判定は執筆計画を考えるための目安です。体調、推敲、資料調査、構成変更に必要な時間は含まれないため、余裕を持った締切設定を推奨します。

### 7.3 日別執筆履歴

本文の文字数が増えると、増加分が当日の日別執筆履歴へ記録されます。右側の「日別執筆履歴」カードには、直近7日間の文字数が棒グラフで表示されます。履歴は作品ごとに分離されます。

## 8. 締切前の通知

締切まで7日以内になると、右側の作品の現在地に締切通知が表示されます。締切まで3日以内、または締切日を過ぎた場合は、緊急色で表示されます。通知を押すと設定画面が開き、締切日、目標文字数、応募先を確認できます。

通知は作品ごとに判定されます。作品棚で別作品へ切り替えると、その作品の締切状況に応じた表示へ切り替わります。

## 9. 保存と外部エディタ互換

### 9.1 JSON保存

「JSON保存」は、Archive Deskの構成、本文、設定、進捗情報を一つのJSONファイルへ保存します。プロジェクトのバックアップや別環境への移動に利用できます。

### 9.2 互換フォルダー保存

「互換保存」またはファイルメニューの「互換フォルダーへ保存」を選択すると、指定フォルダーに次の構造が作成されます。

```text
作品フォルダー/
├─ README.md
├─ .archive-desk/
│  └─ project.json
└─ manuscript/
   └─ アウトライン順のMarkdownファイル
```

`manuscript/`以下の本文は通常のMarkdownです。VS Codeでは「フォルダーを開く」、Obsidianでは「保管庫としてフォルダーを開く」を選び、作品フォルダーを指定してください。

### 9.3 外部変更の扱い

VS CodeやObsidianで本文を変更すると、統合Windowsエディタが外部変更を検知します。現在の編集を残す場合は「保持」、外部ファイルを取り込む場合は「再読込」、内容を比較する場合は「差分」を選択してください。

### 9.4 自動保存と保存先

編集後は、共通のプロジェクト状態がローカルへ自動保存されます。Windows版で互換フォルダーを開いている場合は、そのフォルダーの`.archive-desk/project.json`と`manuscript/`以下のMarkdownも自動更新されます。ヘッダーの「自動保存 HH:MM」で直近の保存時刻を確認できます。

iCloudを利用できない場合でも、初回の保存先案内でこのPC上の任意のフォルダーを選択できます。iCloudは必須ではありません。保存先を後で決める場合も、アプリ内のローカル自動保存が先に働きます。

JSONと互換フォルダーは同じ共通データから生成されます。JSONは作品全体の構成・設定・進捗をまとめたバックアップであり、互換フォルダーは本文を通常のMarkdownとして外部アプリへ持ち出すための形式です。互換フォルダー内にも管理情報として`.archive-desk/project.json`が含まれます。

この方式は、本文をこのアプリだけで読める独自ファイルに閉じ込めないためのものです。本文はプレーンテキストとして残し、構成・目標・締切・執筆履歴など、一般的なテキストエディタが持たない長編執筆用の情報は別の管理データで保持します。VS CodeやObsidianでは本文を自由に編集し、統合Windowsエディタへ戻ったときに構成と進捗を復元できます。

重要な変更の前には、アプリ内のスナップショット、JSON保存、作品フォルダーの複製を組み合わせることを推奨します。

## 10. 書き出し

### 完成原稿のテキスト書き出し

「完成原稿を書き出す」を押すと、アウトライン順に本文を結合した一つのテキストファイルを作成します。章・節・シーンの順序を確認してから実行してください。

### DOCX書き出し

「DOCX」を押すと、本文をWord形式で書き出します。ルビ記法は読みやすい表記へ変換されます。書き出したDOCXは、最終的な体裁調整や提出前の校正に利用できます。

## 11. 変更履歴と復元

履歴ボタンから、版情報、変更履歴、アプリ内スナップショットを確認できます。改定前には「現在を保存」でスナップショットを作成し、問題が生じた場合は対象スナップショットの「復元」を選択してください。

大きな改定を行う際は、スナップショットだけでなく、作品フォルダーそのものを複製して保管してください。

## 12. トラブルシューティング

| 症状 | 対処 |
|---|---|
| Fileメニューに項目が表示されない | 最新版の`.exe`を起動しているか確認し、古いポータブル版を終了してから再生成版を起動する |
| `vite`が見つからない | プロジェクトフォルダーで`pnpm.cmd install`を実行する |
| `pnpm`が認識されない | `npm.cmd install --global pnpm`を実行し、PowerShellでは`pnpm.cmd`を使う |
| 互換フォルダーを開けない | ルート直下に`.archive-desk/project.json`があるか確認する |
| 外部変更の内容が反映されない | 通知の「差分」を確認し、必要に応じて「再読込」を選択する |
| Windowsビルドが途中で止まる | `desktop:win:staged`を使用し、古いビルドプロセスを終了してから再実行する |

## 13. 現段階版の注意事項

現段階版は、長編小説の構成管理と執筆を開始できる実用試作版です。Windows上の実機確認では、IME入力、縦書きスクロール、DOCX書き出し、VS Code・Obsidianとの往復編集、締切通知の表示を重点的に確認してください。

作品の削除・複製・並べ替え、OSの通知領域を使ったバックグラウンド通知、詳細な日別履歴のCSV出力などは今後の拡張候補です。
